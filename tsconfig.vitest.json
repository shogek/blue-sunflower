{
  "extends": "./tsconfig.app.json",
  "exclude": [],
  "compilerOptions": {
    "composite": true,
    /* "DOM" value allows `console.log` statements */
    "lib": ["DOM"],
    "types": ["node", "jsdom"]
  }
}
